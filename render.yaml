services:
  - type: web
    name: planning-poker
    runtime: node
    plan: free
    buildCommand: tar -xzf node_modules.tgz && node -e "require.resolve('express'); require.resolve('socket.io')"
    startCommand: node server.js
    healthCheckPath: /health

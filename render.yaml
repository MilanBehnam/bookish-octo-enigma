services:
  - type: web
    name: planning-poker
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev --no-audit --no-fund && node -e "require.resolve('express'); require.resolve('socket.io')"
    startCommand: npm start
    healthCheckPath: /health

{
  "name": "planning-poker-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Planning poker with a relay backend for bank networks",
  "main": "server.js",
  "engines": {
    "node": ">=20.19.5 <21"
  },
  "scripts": {
    "start": "npm install --omit=dev --no-audit --no-fund --ignore-scripts && node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
